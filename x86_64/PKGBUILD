pkgname=gorillaos-hello
pkgver=1.0
pkgrel=1
pkgdesc="Test OTA package for GorillaOS"
arch=('any')
source=()

package() {
  mkdir -p "$pkgdir/usr/bin"
  echo '#!/bin/bash' > "$pkgdir/usr/bin/gorilla-hello"
  echo 'echo "🦍 OTA Update Infrastructure is ONLINE!"' >> "$pkgdir/usr/bin/gorilla-hello"
  chmod +x "$pkgdir/usr/bin/gorilla-hello"
}
